const types = {
  login: "[auth] login",
  register: "[auth] register",
  logout: "[auth] logout",
  fetchPro: "[products] fetch",
  addPro: "[products] add",
  getCarrito: "[cart] get",
  addCompra: "[cart] compra",
}

export default types;
